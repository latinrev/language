import React from "react";
import ReactDOM from "react-dom/client";
import LinguaApp from "./LinguaApp";
import "./index.css";

ReactDOM.createRoot(document.getElementById("root")!).render(
  <React.StrictMode>
    <LinguaApp />
  </React.StrictMode>,
);
